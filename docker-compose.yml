services:
  app:
    image: video-meeting-composer:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: video-composer
    ports:
      - "8501:8501"
    environment:
      - PORT=8501
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s